//This will also resolve symbols and arithmetic. The order of operations should be defined here.
//Should also make sure spaces are resolved. Functions and variable names should be correctly spaced. 
//There should be a boolean function which returns false if any errors are found, as well as printing the error messages to a log.
