
#include <iostream>
using namespace std;

public class token
{

    T type;
    T value;
    template <typename T>;


    public token(T type, T value)
    {

    }
}

enum Tokens
{
    KEYW, OP, LTR, COM, SEP, ID
};